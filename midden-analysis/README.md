Midden Analysis
=================

This package contains various components written in Rust to help
Midden analyse heap dumps. You probably won't need this package directly,
since it will be installed automatically if you install `midden[ui]`.
